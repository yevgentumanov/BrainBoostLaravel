document.addEventListener('DOMContentLoaded', () => {
    // $(document).ready(() => {
    //     // Evento clic en botones de submenú
    //     $('.navbar-toggler.nav-ico').on('click', () => {
    //         const target = $(this).data('target');

    //         // Contraer todos los submenús
    //         $('.navbar-collapse.show').collapse('hide');

    //         // Expandir el submenú correspondiente al botón actual
    //         if (!$(target).hasClass('show')) {
    //             $(target).collapse('show');
    //         }
    //     });

    //     // Evento clic en área en blanco de la página
    //     $('#navbarCategorias').on('click', (e) => {
    //         // Contraer todos los submenús si se hace clic en una parte en blanco de la página
    //         if ($(e.target).attr('id') === 'navbarCategorias') {
    //             $('.navbar-collapse.show').collapse('hide');
    //         }
    //     });
    // });

});